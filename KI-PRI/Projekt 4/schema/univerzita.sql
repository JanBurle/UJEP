USE univerzita;

CREATE TABLE `fakulta` (
  `a` int NOT NULL,
  `b` int NOT NULL,
  `c` int NOT NULL,
  `d` int NOT NULL
);


INSERT INTO `fakulta` (`a`, `b`, `c`, `d`) VALUES
(1, 2, 3, 4),
(5, 6, 7, 8);
