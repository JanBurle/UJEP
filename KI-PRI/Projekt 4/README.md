Changes:

* Dockerfile moved to Dockerfiles/WebServer
* php/src renamed to html/ to correspond to the inner /var/www/html

docker ls
docker exec -ti <id> bash

apt install software-properties-common
apt install mc nano

/etc/apache2
webserver document root folder
/var/log/apache2

php -v
php -i
/usr/local/etc/php
top
